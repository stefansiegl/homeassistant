# Rclone-Remotes hier oder über Add-on Web UI anlegen:
# Einstellungen → Add-ons → Rclone Backup → Open Web UI → Configs → Create new config
# Remote-Name für Jobs: google (type = drive)
# Nach OAuth: dry_run im Add-on auf false, Add-on neu starten.
