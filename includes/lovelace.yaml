# keep default mode as storage, meaning that I can create dashboards in the UI
mode: storage

# add YAML mode dashboard, with the benefit that those are then easier
# for configuration management.
dashboards:
  tablett-dashboard:
    mode: yaml
    title: Tablett
    icon: mdi:tablet-dashboard
    show_in_sidebar: true
    filename: includes/lovelace/tablett-dashboard.yaml

resources:
  - url: /hacsfiles/lovelace-multiple-entity-row/multiple-entity-row.js
    type: module
