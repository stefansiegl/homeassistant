title: Bewässerung
path: watering-details
icon: mdi:watering-can
layout: !include ../helpers/two-column-layout.yaml
badges: []
theme: Backend-selected
type: custom:grid-layout
cards:
  # --------------------------------------------------------
  # Header
  # --------------------------------------------------------
  - type: horizontal-stack
    cards:
      - type: vertical-stack
        cards:
          - !include ../cards/navigation.yaml
    view_layout:
      grid-area: header

  # --------------------------------------------------------
  # Left
  # --------------------------------------------------------

  - type: vertical-stack
    cards:
      - !include ../cards/irrigation-status-card.yaml
    view_layout:
      grid-area: left

  # --------------------------------------------------------
  # Right
  # --------------------------------------------------------

  - type: vertical-stack
    cards:
      - !include ../cards/irrigation-enable-card.yaml
    view_layout:
      grid-area: right

  # --------------------------------------------------------
  # Footer
  # --------------------------------------------------------
  - type: vertical-stack
    cards:
      - !include ../cards/irrigation-details-zones.yaml
      - !include ../cards/irrigation-adjust-card.yaml
      - !include ../cards/irrigation-details-zones-setup.yaml
    view_layout:
      grid-area: footer
